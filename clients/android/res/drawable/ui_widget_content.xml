<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/widget.content.background"/>

    <stroke
            android:width="@dimen/ui.widget.border"
            android:color="@color/widget.content.border"/>

    <padding
            android:left="@dimen/ui.widget.radius"
            android:top="@dimen/ui.widget.radius"
            android:right="@dimen/ui.widget.radius"
            android:bottom="@dimen/ui.widget.radius"/>

    <corners
            android:bottomRightRadius="@dimen/ui.widget.radius"
            android:bottomLeftRadius="@dimen/ui.widget.radius"
            android:topLeftRadius="@dimen/ui.widget.radius"
            android:topRightRadius="@dimen/ui.widget.radius"/>
</shape>