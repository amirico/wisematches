package wisematches.server.web.services.notify.distributor;

/**
 * Implementation of {@code NotificationDistributor} that doesn't send some messages if they were already
 * sent while player is offline.
 *
 * @author Sergey Klimenko (smklimenko@gmail.com)
 */
public class SpamlessNotificationDistributor {
}
