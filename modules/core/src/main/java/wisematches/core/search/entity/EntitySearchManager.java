package wisematches.core.search.entity;

import wisematches.core.search.SearchManager;

/**
 * @author Sergey Klimenko (smklimenko@gmail.com)
 */
public interface EntitySearchManager<E, C> extends SearchManager<E, C> {
}
