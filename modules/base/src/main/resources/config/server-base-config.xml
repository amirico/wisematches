<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="lockAccountManager" class="wisematches.server.testimonial.lock.impl.HibernateLockAccountManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="accountManager" class="wisematches.server.player.member.impl.HibernateAccountManager">
        <property name="sessionFactory" ref="sessionFactory"/>
        <property name="lockAccountManager" ref="lockAccountManager"/>
    </bean>

    <bean id="computerPlayerManager" class="wisematches.server.player.computer.ComputerPlayerManager"/>

    <bean id="playerManager" class="wisematches.server.player.CompositePlayerManager">
        <property name="playerManagers">
            <list>
                <!-- must be first because very fast -->
                <ref bean="computerPlayerManager"/>
                <ref bean="accountManager"/>
            </list>
        </property>
    </bean>

    <!--<alias alias="playerManager" name="accountManager"/>-->

    <!--    <bean id="ratingsManagerDao" class="wisematches.server.testimonial.rating.impl.RatingsManagerDao">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="lexicalDictionary" class="wisematches.server.games.dictionary.impl.file.FileDictionaryManager">
        <property name="dictionariesFolder" value="${server.dictionary.path}/lexical"/>
    </bean>

    <bean id="slanglyDictionary" class="wisematches.server.games.dictionary.impl.file.FileDictionaryManager">
        <property name="dictionariesFolder" value="${server.dictionary.path}/slangly"/>
        <property name="createEmptyDictionary" value="true"/>
    </bean>

    <bean id="wordGamesDictionaries" class="wisematches.server.games.dictionary.impl.WordGamesDictionariesImpl">
        <property name="lexicalManager" ref="lexicalDictionary"/>
        <property name="slanglyManager" ref="slanglyDictionary"/>
    </bean>

    <bean id="lockAccountManager" class="wisematches.server.player.locks.impl.HibernateLockAccountManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="accountManager" class="wisematches.server.core.account.impl.HibernateAccountManager">
        <property name="sessionFactory" ref="sessionFactory"/>

        <property name="lockAccountManager" ref="lockAccountManager"/>
        <property name="defaultPlayerRating" value="1200"/>
    </bean>

    <bean id="statisticsManager" class="wisematches.server.testimonial.statistic.impl.HibernatePlayerStatisticsManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="serverPropertiesManager" class="wisematches.server.utils.system.impl.HibernateServerPropertiesManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="countriesManager" class="wisematches.server.testimonial.profile.counties.impl.CSVCountriesManager">
        <property name="csvCountriesFile" value="classpath:/i18n/listOfCountries.csv"/>
    </bean>-->
</beans>