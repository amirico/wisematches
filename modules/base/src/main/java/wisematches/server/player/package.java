/**
 * The package contains set of interfaces and classes to work with {@code Player} objects.
 *
 * This package also contains a set of subpackages for working with player information, like
 * player's profile, statistic and so on.
 */
package wisematches.server.player;