package wisematches.server.deprecated.web.modules.app.sessions;

/**
 * @author <a href="mailto:smklimenko@gmail.com">Sergey Klimenko</a>
 */
public class MockEvent { //implements Event, Correlative {
/*
    private Map<Event, Correlation> correlations = new HashMap<Event, Correlation>();

    public MockEvent() {
    }

    public MockEvent correlate(Event e, Correlation correlation) {
        correlations.put(e, correlation);
        return this;
    }

    public boolean isEventForPlayer(long playerId) {
        return true;
    }

    public Correlation eventCorrelation(Event event) {
        final Correlation eventCorrelation = correlations.get(event);
        return eventCorrelation != null ? eventCorrelation : Correlation.NEUTRAL;
    }
*/
}
