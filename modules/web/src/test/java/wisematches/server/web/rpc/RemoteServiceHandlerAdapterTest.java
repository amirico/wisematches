package wisematches.server.web.rpc;

import static org.easymock.EasyMock.createNiceMock;
import static org.junit.Assert.*;
import org.junit.Test;

/**
 * @author <a href="mailto:smklimenko@gmail.com">Sergey Klimenko</a>
 */
public class RemoteServiceHandlerAdapterTest {
/*
    @Test
    public void testSupports() throws Exception {
        final RemoteServiceHandlerAdapter adapter = new RemoteServiceHandlerAdapter();

        assertFalse(adapter.supports(createNiceMock(Object.class)));

        assertTrue(adapter.supports(createNiceMock(RemoteService.class)));
        assertTrue(adapter.supports(createNiceMock(SecureRemoteService.class)));
        assertTrue(adapter.supports(createNiceMock(GenericRemoteService.class)));
        assertTrue(adapter.supports(createNiceMock(GenericSecureRemoteService.class)));
    }

    @Test
    public void testGetLastModified() throws Exception {
        final RemoteServiceHandlerAdapter handlerAdapter = new RemoteServiceHandlerAdapter();
        assertEquals(-1, handlerAdapter.getLastModified(null, null));
    }
*/
}
